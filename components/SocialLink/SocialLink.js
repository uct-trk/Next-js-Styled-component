import { Link } from "./iconStyle";

const SocialLink = ({ image }) => {
  return (
    <Link href="https://www.starwars.com/" image={image}>
      <span />
    </Link>
  );
};

export default SocialLink;
